import { mockProviders } from '../mocks/providers';
import { Environment } from './type';

export const environment: Environment = {
  production: false,
  assetsStaticItemName: 'bb-retail-app-ang',
  mockProviders,
};
